// Copy this file to config/wifi_credentials.h and fill in your WiFi credentials.
// The config/ directory is ignored by git (see .gitignore).

#pragma once

// Example WiFi credentials (do not commit real secrets)
#undef WIFI_SSID
#undef WIFI_PASSWORD
#define WIFI_SSID "YourSSID"
#define WIFI_PASSWORD "YourPassword"
